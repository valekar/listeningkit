# OS
.DS_Store

# Python
__pycache__/
*.py[codz]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/

# Environment files
.env
.env.local
.env.bak
*.bak

# Django
*.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Local background job tickers (make up-jobs): per-stage pid + log
.jobs/

# Celery
celerybeat-schedule
celerybeat.pid

# IDE
.idea/
.vscode/
.claude/

# Caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Web (Next.js / pnpm)
node_modules/
.next/
.turbo/
.pnpm-store/
*.tsbuildinfo

# CLI build artifacts
cli/dist/
cli/.venv/

# config/ holds the user's feed/watch configs. The committed README + templates
# stay tracked; everything else (the seeded quickstart/ instance + anything the
# user writes) is theirs and ignored.
/config/*
!/config/README.md
!/config/templates/

# Local export artifacts from `magpie activity export -o <file>` (CSV / NDJSON).
# Generated data dumps, not source; root-anchored so test fixtures under
# apps/*/tests/ are unaffected. Force-add (`git add -f`) if you ever need one.
/*.csv
/*.ndjson
/*.jsonl

# local-only: override docker-compose ports when the host ports are taken (see docker-compose.override.yml)
docker-compose.override.yml

# Host ACP bridge service logs
/integrations/acp-bridge/.logs/
